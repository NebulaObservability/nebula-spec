# Specification Sources

此目录只存放生成输入，不存放手写的语言 DTO。

- `apm/v1/`：APM OTLP 约束、资源要求和厂商扩展。
- `control-plane/v1/`：服务端配置修订与 SDK 配置投递边界。
- `rum/rutp/v1/`：RUTP v1 Protobuf。
- `semantics/`：属性、事件、指标、Span 和枚举注册表。
- `privacy/`：数据分类和默认脱敏策略。
- `otlp/`：标准信号映射。
